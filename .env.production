# Defaults for `npm run build`.
# Without these a bare build silently falls back to the wrong API host and the
# live site shows "network error" on every message.
VITE_ANDY_MOCK=0
VITE_ANDY_API=https://andy-api-production.up.railway.app
